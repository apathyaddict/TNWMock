import { StyleSheet, SafeAreaView, Text, Button } from "react-native";

export default function CookNowScreen() {
  return (
    <SafeAreaView>
      <Text>This is the CookNow Screen</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});
