import { StyleSheet, SafeAreaView, Text } from "react-native";

export default function MyRecipesScreen() {
  return (
    <SafeAreaView>
      <Text>This is the My Recipes Screen</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});
